# Environment variables for the admin backend
PORT=3000
JWT_SECRET=change-me-in-production
DB_PATH=./data/data.sqlite

# If set, the project will seed an initial admin user on startup
# ADMIN_USER=admin
# ADMIN_PASSWORD=password123

# Add other settings below (database connection string, secrets, etc.)
